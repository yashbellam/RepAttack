# Copy this file to keystore.properties and fill in your values.
# keystore.properties is gitignored — never commit it.
# Without this file, release builds fall back to the debug signing key.

storeFile=C:/absolute/path/to/repattack-release.jks
storePassword=
keyAlias=repattack
keyPassword=
